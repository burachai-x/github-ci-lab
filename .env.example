# ไฟล์ตัวอย่างสำหรับคัดลอกไปเป็น .env ในเครื่องตัวเอง (.env ถูก ignore ไว้แล้ว)
# ห้ามใส่ค่าจริงลงในไฟล์นี้ — ค่าจริงอยู่ใน secret manager หรือ GitHub Actions secrets
PORT=3000
NODE_ENV=development
API_TOKEN=ใส่ค่าจากผู้ดูแลระบบ
